pipeline {
    agent {
        docker {
            image 'node:12.6.0' 
            args '-p 3000:3000' 
        }
    }
    stages {
        stage('Build') { 
            steps {
                sh 'echo working!! Node version is $(node -v)' 
            }
        }
    }
}